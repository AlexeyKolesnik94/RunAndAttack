using Damage;
using UnityEngine;

namespace Enemy
{
    [RequireComponent(typeof(DestroyEnemies), typeof(Hunting),
        typeof(DamageSystem))]
    public class Enemy : MonoBehaviour
    {
        
    }
}